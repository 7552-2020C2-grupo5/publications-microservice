build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - poetry run python publications_microservice/manage.py db upgrade
run:
  web: poetry run gunicorn -w 2 --bind 0.0.0.0:$PORT "publications_microservice.app:create_app()"
